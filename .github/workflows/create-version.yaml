name: Create version

on:
  push:
    branches:
      - master

jobs:
  runs-on: ubuntu-latest
  build:
    steps:
    - name: yarn install and test
      run: echo 'Create new version only for masrer push'
