import _extends from "@babel/runtime/helpers/extends";
import _objectWithoutProperties from "@babel/runtime/helpers/objectWithoutProperties";
import * as React from 'react';
import styled from 'styled-components';
const StyledIcon = React.forwardRef((props, ref) => {
  const title = props.title,
        size = props.size,
        svgProps = _objectWithoutProperties(props, ["title", "size"]);

  const iconProps = {
    viewBox: '0 0 24 24',
    height: props.height !== undefined ? props.height : size,
    width: props.width !== undefined ? props.width : size,
    // @ts-ignore - aria is not always defined on SVG in React TypeScript types
    'aria-hidden': title == null ? 'true' : undefined,
    focusable: 'false',
    role: title != null ? 'img' : undefined,
    "fill": "currentColor"
  };
  return React.createElement("svg", _extends({}, iconProps, svgProps, {
    ref: ref
  }), title && React.createElement("title", {
    key: "FiberManualRecord-title"
  }, title), React.createElement("circle", {
    cx: 12,
    cy: 12,
    r: 8,
    fill: "#010101",
    key: "k0"
  }));
});
export const FiberManualRecord = styled(StyledIcon).withConfig({
  displayName: "FiberManualRecord",
  componentId: "rck9c0-0"
})(["display:inline-block;vertical-align:middle;overflow:hidden;"]);
FiberManualRecord.displayName = 'FiberManualRecord';
export const FiberManualRecordDimensions = {
  height: 24,
  width: 24
};